import { NotificationContent, NotificationRecipients, Notifications } from "@prisma/client";

export const accountValidation = async(noti: Notifications, content: NotificationContent, recipients: NotificationRecipients[]) => {
    return ""
}